fastapi>=0.115.0
uvicorn[standard]>=0.32.0
websockets>=13.0
pydantic>=2.10.0
aiofiles>=24.0.0
python-dotenv>=1.0.0
pytest>=8.3.0
pytest-asyncio>=0.24.0
httpx>=0.27.0
requests>=2.31.0
cryptography>=41.0.0
keyring>=25.0.0          # Secure credential storage in OS keychain

# Audio/Voice Processing
pyaudio>=0.2.11
numpy>=1.24.0
scipy>=1.11.0
pvporcupine>=4.0.0       # wake word detection; v4+ required for .ppn v4 model files. Code handles v1.x (no key) and v2+/v4+ (needs PICOVOICE_ACCESS_KEY)
sounddevice>=0.5.0
# PyTorch with CUDA support (GPU)
# For GPU users: install with CUDA using:
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121
# For CPU-only users: keep as is
torch>=2.0.0
torchaudio>=2.0.0
psutil>=5.9.0
# STT Pipeline — RealtimeSTT bundles faster-whisper + silero-VAD + webrtcvad.
# Replaces the previous LFM audio model + manual VAD + fallback chain.
RealtimeSTT>=0.3.0       # real-time STT: faster-whisper/tiny + silero-VAD (offline)
faster-whisper>=1.0.0    # pinned as RealtimeSTT dependency (CPU int8, ~40 MB model)

# TTS Pipeline — LuxTTS voice cloning (Python 3.13 compatible) + pyttsx3 fallback
# LuxTTS core deps (install via: pip install git+https://github.com/ysharma3501/LinaCodec.git && pip install git+https://github.com/ysharma3501/LuxTTS.git --no-deps)
torch>=2.0.0             # already listed above; LuxTTS also uses torch
torchaudio>=2.0.0        # already listed above
huggingface_hub>=0.20.0  # model download from YatharthS/LuxTTS
safetensors>=0.4.0
vocos>=0.1.0
lhotse>=1.30.0
librosa>=0.10.0
onnxruntime>=1.17.0
pydub>=0.25.0
piper-phonemize-fix>=1.2.2  # Python 3.13 wheels; drop-in for piper-phonemize
"transformers<=4.57.6"   # pinned: LuxTTS requires <=4.57.6
cn2an>=0.5.0
inflect>=7.0.0
jieba>=0.42.0
pypinyin>=0.50.0
pyttsx3>=2.90            # SAPI5 built-in fallback TTS (zero download)

# Vision & Automation (Phase 2)
mss>=9.0.0
Pillow>=10.0.0
pyautogui>=0.9.54
anthropic>=0.18.0
openai>=1.0.0

# IRIS Memory Foundation (Episodic & Semantic Memory)
sqlcipher3>=0.5.0        # AES-256 encrypted SQLite (requires libsqlcipher-dev)
sqlite-vec>=0.1.0        # Vector similarity search in SQLite
sentence-transformers>=2.7.0  # all-MiniLM-L6-v2 embedding model