fastapi>=0.115.0
uvicorn[standard]>=0.32.0
websockets>=13.0
pydantic>=2.10.0
aiofiles>=24.0.0
python-dotenv>=1.0.0
pytest>=8.3.0
pytest-asyncio>=0.24.0
httpx>=0.27.0
requests>=2.31.0
cryptography>=41.0.0
keyring>=25.0.0          # Secure credential storage in OS keychain

# Audio/Voice Processing
pyaudio>=0.2.11
numpy>=1.24.0
scipy>=1.11.0
pvporcupine>=4.0.0       # wake word detection; v4+ required for .ppn v4 model files. Code handles v1.x (no key) and v2+/v4+ (needs PICOVOICE_ACCESS_KEY)
sounddevice>=0.5.0
# PyTorch with CUDA support (GPU)
# For GPU users: install with CUDA using:
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121
# For CPU-only users: keep as is
torch>=2.0.0
torchaudio>=2.0.0
psutil>=5.9.0
# STT Pipeline — RealtimeSTT bundles faster-whisper + silero-VAD + webrtcvad.
# Replaces the previous LFM audio model + manual VAD + fallback chain.
RealtimeSTT>=0.3.0       # real-time STT: faster-whisper/tiny + silero-VAD (offline)
faster-whisper>=1.0.0    # pinned as RealtimeSTT dependency (CPU int8, ~40 MB model)

# TTS Pipeline — CosyVoice2-0.5B zero-shot voice cloning + streaming
# Model lives at: backend/voice/CosyVoice/ (cloned repo)
# Pretrained model: backend/voice/pretrained_models/CosyVoice2-0.5B
# Install CosyVoice deps:
#   pip install conformer hydra-core hyperpyyaml "x-transformers>=2.0" "diffusers==0.29.0"
torch>=2.0.0             # already listed above
torchaudio>=2.0.0        # already listed above
conformer>=0.3.2
hydra-core>=1.3.2
hyperpyyaml>=1.2.3
x-transformers>=2.0.0
diffusers==0.29.0

# Fallback TTS — pyttsx3 built-in fallback (zero download, instant)
pyttsx3>=2.90            # SAPI5 built-in fallback TTS (Windows only)

# Local GGUF inference — llama-cpp-python server mode on port 8082
llama-cpp-python>=0.2.0  # GGUF model server (optional — install when using iris_local provider)
huggingface-hub>=0.20.0  # Model download from HuggingFace Hub
