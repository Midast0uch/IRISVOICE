# dependencies
/node_modules
/.pnp
.pnp.js

# testing
/coverage

# next.js
/.next/
/out/
/dist

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files
.env
.env*.local

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# IDE
.vscode/
.idea/

# logs
/logs
*.log

# IRIS-specific
*.db
*.db-shm
*.db-wal
/bootstrap/*.db
/bootstrap/*.db-shm
/bootstrap/*.db-wal
.mcm/

# Session state files contain API keys
/backend/sessions/

# Benchmark secrets
.env.benchmark

# API keys / model config (contains secrets)
data/iris_config.json

# Dilithium Post-Quantum identity private key — never commit the real key.
# Loaded at runtime via IRIS_DILITHIUM_KEY (env hex) or IRIS_DILITHIUM_KEY_FILE
# (an external path you supply). Raw key-byte files must never enter the repo.
*.key

# Windows nul file (reserved name bug)
nul
NUL

# screenshots (generated)
/screenshots

# test artifacts
test_output.txt
test.txt
test_image.png

# tailscale
tailscale_active.png

# llama.cpp forks (external dependencies, not committed)
llama.cpp/build/

# Bundled llama-server (built artifact shipped via Tauri bundle.resources)
src-tauri/binaries/llama-server/
llama.cpp/CMakeCache.txt
llama.cpp/CMakeFiles/
ik_llama.cpp/build_ik/
ik_llama.cpp/CMakeCache.txt
ik_llama.cpp/CMakeFiles/
llama-cpp-turboquant/

# build scripts (generated)
build_*.bat
bench_*.bat
**/__pycache__/
